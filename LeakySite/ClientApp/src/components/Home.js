import React, { Component } from 'react';

export class Home extends Component {
  static displayName = Home.name;

  render () {
    return (
      <div>Hello! Much of this template will remain untouched. The focus of this repository is to determine what attack vectors exist in dotnet core azure hosted projects.</div>
    );
  }
}
